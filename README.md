```
$ pip install -r requirements.txt
$ ./convert.py -h
usage: convert.py [-h] -t TEAM -f FILE -k KEY

optional arguments:
  -h, --help            show this help message and exit
  -t TEAM, --team TEAM  team name
  -f FILE, --file FILE  backup filepath
  -k KEY, --key KEY     apikey
```